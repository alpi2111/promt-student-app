import 'package:flutter/material.dart'; 

class Localizacion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Container(
        child: Text ('Aqui iran las entradas y salidas'),
      ),
    );
  }
}